# Quickstart sample

Import this sample from Package Manager. Create a `SupabaseSettings` asset, assign it to the component, and create a `public.scores` table with matching RLS select policy. Attach `SupabaseQuickstart` to a GameObject.
